spring:
  application:
    name: edi-clean-arch-test

  datasource:
    url: jdbc:sqlserver://localhost;databaseName=CDB;encrypt=false;trustServerCertificate=true
    username: sa
    password: 1111
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      auto-offset-reset: earliest
      enable-auto-commit: false

mybatis:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.example.edicleanarch: DEBUG
    org.springframework.jdbc: DEBUG
